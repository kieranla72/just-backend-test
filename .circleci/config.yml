version: 2.1
jobs:
  run_tests:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run: npm install --save
      - run: npm test
workflows:
  build_test:
    jobs:
      - run_tests